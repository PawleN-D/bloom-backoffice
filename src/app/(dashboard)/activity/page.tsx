import PlaceholderPage from "@/components/PlaceholderPage";

export default function ActivityPage() {
  return (
    <PlaceholderPage
      title="Activity Logs"
      description="Audit admin actions, organization changes, and billing events."
    />
  );
}
